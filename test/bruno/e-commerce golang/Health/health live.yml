info:
  name: health live
  type: http
  seq: 2

http:
  method: GET
  url: localhost:8080/health/live
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
